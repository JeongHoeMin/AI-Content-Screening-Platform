# Claude Code Project Instructions

이 프로젝트에서 파일을 생성, 수정 또는 삭제하는 모든 작업은 프로젝트 루트의 `AGENTS.md`에 정의된 공통 작업 관리 규칙을 반드시 따른다.

## 작업 시작 절차

사용자의 요청을 받은 후 다음 순서로 작업한다.

1. 프로젝트 루트의 `AGENTS.md`를 읽는다.
2. 요청이 파일 생성, 수정 또는 삭제를 필요로 하는지 판단한다.
3. 파일 변경이 필요하면 `AGENTS.md`에 정의된 작업 디렉토리와 `task.yaml`을 먼저 생성한다.
4. `task.yaml`의 상태와 작업 범위를 갱신하면서 작업한다.
5. 테스트와 검증이 완료된 경우에만 작업을 완료한다.

## Hook 적용 상태

이 프로젝트에는 현재 hook-test 프로젝트와 달리 `task.yaml` 없는 파일 변경을 자동으로 차단하는 Hook이 연결되어 있지 않다. 즉 `AGENTS.md`의 작업 절차는 Claude Code가 자율적으로 준수해야 하는 규칙이며, 도구 호출 단계에서 강제로 차단되지 않는다. Hook 연동은 사용자가 별도로 요청하기 전까지 진행하지 않는다.

## 우선순위

Claude Code 전용 지침과 `AGENTS.md`의 공통 작업 규칙이 충돌하는 경우 다음 우선순위를 따른다.

1. 사용자가 현재 대화에서 명시적으로 내린 지시
2. 이 파일에 정의된 Claude Code 전용 지침
3. 프로젝트 루트의 `AGENTS.md`
4. 그 밖의 일반적인 작업 방식

충돌이 명확하지 않으면 규칙을 임의로 무시하지 않고 사용자에게 알린다.

## 규칙 우회 금지

다음 행동을 하지 않는다.

* `AGENTS.md`를 읽지 않고 파일 변경을 시작하는 행위
* 작업 디렉토리와 `task.yaml` 생성을 생략하는 행위
* 작업 상태와 허용 경로를 무시하는 행위
* 테스트나 검증 없이 작업을 완료했다고 보고하는 행위
* 사용자의 명시적인 요청 없이 작업 관리 규칙을 수정하거나 비활성화하는 행위
* 향후 Hook이 추가된 이후 Hook 또는 검증 정책을 우회하는 행위

공통 작업 절차와 세부 상태 관리 방법은 중복해서 정의하지 않고 `AGENTS.md`를 단일 기준으로 사용한다.
