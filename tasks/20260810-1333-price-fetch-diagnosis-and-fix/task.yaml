schemaVersion: 1

id: "20260810-1333-price-fetch-diagnosis-and-fix"
title: "매수/매도 추천 현재가 조회 원인 조사 및 KIS 토큰 갱신 버그 수정"
description: |
  사용자가 매수/매도 추천 시 현재 주가가 표시되지 않는다고 보고했다. 조사 결과 가격 조회·기록·
  성과 계산 기능(app/market_prices/**, app/harness/recommendation_prices.py,
  recommendation_price_snapshots 테이블, 대시보드의 "가격 성과" 컬럼)은 이미 구현되어 있었다.
  실행 중인 docker 컨테이너의 실제 DB(recommendation_price_snapshots)를 직접 조회한 결과 모든
  스냅샷이 status=unavailable, error_kind=authentication으로 기록되어 있었다. .env의 값을
  노출하지 않고 길이만 확인한 결과 KIS_APP_KEY/KIS_APP_SECRET이 3자리(플레이스홀더로 추정)였고
  KRX_API_KEY(40자리)도 KRX 서버에서 인증 거부를 받고 있었다.
  코드 검토 중 별도 버그를 발견했다: KisRealtimePriceClient는 access_token을 프로세스 수명
  동안 무기한 캐시하며, 캐시된 토큰이 이후(만료 등) 가격 조회 자체에서 401/403을 받아도 토큰을
  무효화하지 않는다. 즉 유효한 키로 한 번 인증에 성공하더라도 토큰이 만료되면 프로세스를 재시작할
  때까지 영구적으로 인증 실패가 반복된다. 이 작업에서는 이 토큰 갱신 버그를 수정하고, "가격
  미확인" 상태의 실패 사유(error_kind)를 API와 대시보드에 노출해 향후 자격 증명 문제를 화면에서
  바로 진단할 수 있게 한다. 사용자가 실제 유효한 KIS/KRX 키를 .env에 반영하면 그 시점에 별도로
  실계약 테스트(RUN_LIVE_MARKET_DATA_TESTS=1)를 수행한다.

status: completed

createdAt: "2026-08-10T13:33:04+09:00"
updatedAt: "2026-08-10T13:52:00+09:00"

agent:
  provider: "claude-code"
  model: "claude-sonnet-5"
  sessionId: null

request:
  summary: "매수/매도 추천 시 현재 주가가 표시되지 않는 원인을 조사하고, 기능이 없다면 추가한다. (사용자 3개 요청 중 우선순위 1번)"
  originalPrompt: "1. 매수/매도 판매 추천 시 현재 주가를 불러와서 보여주는게 지금 안되고 있는데 기능이없어서 안되는건지 왜안되는건지 파악하고 기능이 없다면 추가"

scope:
  allowedPaths:
    - "app/market_prices/kis.py"
    - "app/market_prices/performance.py"
    - "app/harness/recommendation_prices.py"
    - "app/web/dashboard_html.py"
    - "tests/test_kis_market_price_client.py"
    - "tests/test_recommendation_performance.py"
    - "tests/test_dashboard_performance_api.py"
    - "tasks/20260810-1333-price-fetch-diagnosis-and-fix/**"

  deniedPaths:
    - ".env"
    - ".env.*"
    - "secrets/**"
    - ".venv/**"
    - "__pycache__/**"
    - ".git/**"
    - "runtime/**"
    - "dist/**"
    - "build/**"
    - ".agent/**"

requirements:
  items:
    - "매수/매도 추천의 현재가 조회가 왜 동작하지 않는지 원인을 조사하고 결과를 기록한다."
    - "가격 조회 기능 자체가 이미 구현되어 있는지, 없다면 무엇을 추가해야 하는지 결론을 남긴다."
    - "KIS 실시간 가격 클라이언트가 캐시된 access token으로 인증 실패를 겪은 뒤에도 다음 호출에서 새 토큰을 재발급받아 복구하도록 수정한다."
    - "가격 조회가 UNAVAILABLE인 경우 그 실패 사유(error_kind)를 성과 API 응답에 포함한다."
    - "대시보드 UI가 가격 미확인 상태일 때 실패 사유를 사람이 읽을 수 있는 문구로 표시한다."
    - "기존 동작(가격 조회 성공, 성과 계산, 기존 API 응답 필드)을 깨뜨리지 않는다."
    - "새 동작을 검증하는 단위 테스트를 추가하고 관련 테스트 스위트를 실행한다."

plan:
  steps:
    - id: 1
      description: "가격 조회/기록/성과 계산 코드 경로 전체를 읽고 실행 중인 docker 컨테이너의 DB 상태를 직접 조회해 원인을 확정한다."
      status: completed

    - id: 2
      description: "KisRealtimePriceClient가 가격 조회 중 인증 실패를 받으면 캐시된 access_token을 무효화하도록 수정한다."
      status: completed

    - id: 3
      description: "RecommendationPerformanceItem에 entry/latest 실패 사유(error_kind) 필드를 추가하고 harness에서 채운다."
      status: completed

    - id: 4
      description: "대시보드 HTML/JS에서 가격 미확인 사유를 한글 문구로 표시한다."
      status: completed

    - id: 5
      description: "tests/test_kis_market_price_client.py, tests/test_recommendation_performance.py, tests/test_dashboard_performance_api.py에 새 동작 테스트를 추가하거나 갱신한다."
      status: completed

    - id: 6
      description: "uv run pytest 및 uv run python -m compileall app tests로 전체 검증한다."
      status: completed

result:
  summary: >
    가격 조회 기능은 이미 구현되어 있었으나(app/market_prices/**, price snapshot 저장, 성과 API,
    대시보드 표시), 실행 중인 docker 컨테이너 DB를 직접 조회해 모든 가격 스냅샷이
    status=unavailable, error_kind=authentication으로 기록되고 있음을 확인했다. KIS_APP_KEY/
    APP_SECRET은 자리수로 볼 때 플레이스홀더로 보이고 KRX_API_KEY도 서버에서 인증을 거부하고
    있어 근본 원인은 두 외부 시세 공급자의 자격 증명 문제다(사용자가 추후 .env에 실제 키를
    반영하기로 함). 이와 별개로 코드 검토 중 실제 버그를 발견해 수정했다: KisRealtimePriceClient가
    access_token을 프로세스 수명 동안 무기한 캐시하면서, 캐시된 토큰이 이후 가격 조회에서
    401/403을 받아도 무효화하지 않아 유효한 키로 최초 인증에 성공하더라도 토큰이 만료되면
    프로세스 재시작 전까지 영구적으로 인증 실패가 반복되는 문제였다. 이번 수정으로 인증 실패 시
    캐시된 토큰을 무효화해 다음 호출에서 재인증하도록 했다. 또한 "가격 미확인" 상태의 실패
    사유(error_kind)를 성과 API 응답과 대시보드 UI에 노출해 향후 자격 증명 문제를 화면에서 바로
    진단할 수 있게 했다. 실제 유효한 KIS/KRX 키가 .env에 반영되지 않았으므로 실계약(live) 검증은
    수행하지 못했다 — 사용자가 키를 넣었다고 알리면 RUN_LIVE_MARKET_DATA_TESTS=1로 재검증해야 한다.

  changedFiles:
    - "app/market_prices/kis.py"
    - "app/market_prices/performance.py"
    - "app/harness/recommendation_prices.py"
    - "app/web/dashboard_html.py"
    - "tests/test_kis_market_price_client.py"
    - "tests/test_recommendation_performance.py"
    - "tasks/20260810-1333-price-fetch-diagnosis-and-fix/task.yaml"

  verifications:
    - command: "uv run pytest tests/test_kis_market_price_client.py tests/test_recommendation_performance.py tests/test_dashboard_performance_api.py tests/test_recommendation_price_recorder.py tests/test_recommendation_price_repository.py tests/test_market_price_service.py tests/test_krx_market_price_client.py tests/test_telegram_price_summary.py -q"
      result: passed
      summary: "가격/성과 관련 테스트 32개(신규 토큰 무효화 테스트, 신규 error_kind 노출 테스트 포함)가 모두 통과했다."

    - command: "uv run python -m compileall app tests -q"
      result: passed
      summary: "구문 오류 없이 전체 컴파일에 성공했다."

    - command: "uv run pytest -q"
      result: passed
      summary: "전체 스위트 617개 통과, 1개 skip(RUN_LIVE_MARKET_DATA_TESTS opt-in 실계약 테스트, 자격 증명 미설정으로 예상된 skip)."

    - command: "RUN_LIVE_MARKET_DATA_TESTS=1 uv run pytest tests/test_market_price_live_contract.py -q"
      result: passed
      summary: >
        2026-08-10 14:14 사용자가 실제 KIS 자격 증명을 .env에 반영한 뒤 실행했다. 삼성전자(005930)
        실시간 가격이 AVAILABLE·양수로 조회되어 KIS 자격 증명과 어댑터 계약이 정상임을 확인했다.
        이전 completedAt 시점에 skip으로 남아 있던 유일한 미검증 항목이 해소되었다.

    - command: "docker-compose up -d --build dashboard schedule-worker 후 GET /api/runs/history 및 신규 추천 실행"
      result: passed
      summary: >
        실환경 검증. 재배포 후 신규 실행(run 581bf7fd, 14:17 KST)에서 진입가가 kis/realtime으로
        정상 기록되었다(일성건설 1484원→1490원 +0.4%, 다보링크 898원→903원 +0.6%, 확인 2건·승률 100%).
        키가 없던 시절의 과거 회차는 진입가 스냅샷이 불변으로 이미 저장되어 있어 계속 미확인이며,
        이번 작업에서 추가한 error_kind 노출 덕분에 화면에 "가격 미확인 (사유: 인증 실패)"로
        정확한 원인이 표시되는 것을 확인했다.

  completedAt: "2026-08-10T13:52:00+09:00"

blocker:
  reason: null
  requiredAction: null

history:
  - at: "2026-08-10T13:33:04+09:00"
    status: planning
    message: "Task created after confirming with the user that KIS/KRX credentials will be supplied later and that Next.js will replace only the frontend."

  - at: "2026-08-10T13:33:04+09:00"
    status: planning
    message: "Root cause confirmed by querying the live docker postgres container: all recommendation_price_snapshots rows have status=unavailable, error_kind=authentication for both entry and latest snapshots."

  - at: "2026-08-10T13:35:00+09:00"
    status: in_progress
    message: "Implementation started: invalidate cached KIS access_token on authentication failure so the next fetch() re-authenticates instead of repeating the same stale token forever."

  - at: "2026-08-10T13:48:00+09:00"
    status: testing
    message: "Added entry_error_kind/latest_error_kind to RecommendationPerformanceItem and surfaced human-readable failure reasons in the dashboard 가격 성과 column; ran targeted and full pytest suites."

  - at: "2026-08-10T13:52:00+09:00"
    status: completed
    message: "All verifications passed (32 targeted tests, compileall, 617 full suite with 1 expected live-contract skip). Live KIS/KRX credential test deferred until the user confirms .env has real keys."

  - at: "2026-08-10T14:20:00+09:00"
    status: completed
    message: "Deferred live verification resolved. The user supplied real KIS credentials, the opt-in live contract test passed, and after redeploying the containers a fresh run recorded kis/realtime entry prices with computed returns. Verification records appended; task remains completed and no code changed."
