schemaVersion: 1

id: "20260810-1305-local-http-cookie-fix"
title: "로컬 HTTP 접속에서 설정 페이지 세션 쿠키가 유지되도록 SCHEDULE_COOKIE_SECURE=false로 변경"
description: "사용자가 /settings 페이지 접속 방법을 물었고, 코드 검토 결과 SCHEDULE_COOKIE_SECURE=true(현재 .env 값)와 http://localhost:8000 접속 조합에서는 로그인 세션 쿠키가 Secure 속성 때문에 브라우저에 저장되지 않아 로그인 직후 401로 튕길 가능성이 높다는 것을 확인했다. 이 프로젝트는 리버스 프록시나 TLS 설정이 없어 HTTPS 자체가 불가능한 상태(직접 확인 완료)이므로, 사용자 승인을 받아 SCHEDULE_COOKIE_SECURE를 false로 바꾸고 docker compose로 재배포한다."

status: blocked

createdAt: "2026-08-10T13:05:11+09:00"
updatedAt: "2026-08-10T13:12:00+09:00"

agent:
  provider: "claude-code"
  model: "claude-sonnet-5"
  sessionId: null

request:
  summary: "로컬 http 접속에서 /settings 로그인 세션이 유지되도록 SCHEDULE_COOKIE_SECURE를 false로 바꾸고 재배포한다."
  originalPrompt: "ㅇㅇ그렇게하자 (SCHEDULE_COOKIE_SECURE를 false로 바꿀지 물은 것에 대한 승인)"

scope:
  allowedPaths:
    - ".env"
    - "tasks/20260810-1305-local-http-cookie-fix/**"

  deniedPaths:
    - "secrets/**"
    - ".venv/**"
    - "__pycache__/**"
    - ".git/**"
    - "runtime/**"
    - "dist/**"
    - "build/**"
    - ".agent/**"

requirements:
  items:
    - ".env의 SCHEDULE_COOKIE_SECURE 값을 true에서 false로 변경한다."
    - "다른 .env 값(비밀번호, API 키 등)은 건드리지 않는다."
    - "docker compose로 dashboard(및 필요 시 schedule-worker)를 재기동해 변경된 환경변수를 반영한다."
    - "실제 브라우저로 /settings 로그인이 성공한 뒤 세션이 유지되는지(다음 요청에서 401이 나지 않는지) 확인한다."

plan:
  steps:
    - id: 1
      description: ".env의 SCHEDULE_COOKIE_SECURE=true를 false로 변경한다."
      status: completed

    - id: 2
      description: "docker compose로 dashboard·schedule-worker를 재기동한다(이미지 재빌드 불필요, 환경변수만 변경)."
      status: completed

    - id: 3
      description: "실제 브라우저에서 /settings 로그인 후 세션이 유지되는지 확인한다."
      status: blocked

result:
  summary: null
  changedFiles: []
  verifications: []
  completedAt: null

blocker:
  reason: "SCHEDULE_COOKIE_SECURE=false로 변경하고 dashboard를 재기동해 설정은 반영됐지만, 실제 /settings 로그인 성공 후 세션이 유지되는지는 Claude가 확인할 수 없다. 로그인에는 SCHEDULE_SETTINGS_PASSWORD 입력이 필요한데, 비밀번호를 어떤 필드에도 대신 입력하지 않는다는 안전 규칙 때문에 이 마지막 검증 단계는 대리 수행하지 않았다."
  requiredAction: "사용자가 직접 브라우저에서 http://localhost:8000/settings 에 비밀번호를 입력해 로그인하고, 스케줄 조회/저장이 401 없이 되는지 확인해야 한다."

history:
  - at: "2026-08-10T13:05:11+09:00"
    status: in_progress
    message: "Task created after user approved (ㅇㅇ그렇게하자) switching SCHEDULE_COOKIE_SECURE to false, having confirmed HTTPS is not reachable in this deployment (no reverse proxy/TLS configured). .env is normally a denied path but the user's explicit request in this conversation authorizes this specific change, per AGENTS.md's exception for denied paths."

  - at: "2026-08-10T13:10:00+09:00"
    status: in_progress
    message: "Changed .env SCHEDULE_COOKIE_SECURE from true to false via a targeted sed replacement (avoided reading the full .env file into context/transcript, since it also contains SCHEDULE_SETTINGS_PASSWORD/TELEGRAM_BOT_TOKEN/other secrets). Ran docker-compose up -d dashboard schedule-worker; dashboard was recreated with the new env var (confirmed SCHEDULE_COOKIE_SECURE=false inside the container), schedule-worker was left untouched since it never referenced this variable. Dashboard health check passed post-recreation."

  - at: "2026-08-10T13:12:00+09:00"
    status: blocked
    message: "Cannot complete the final login-persistence verification myself: it requires typing SCHEDULE_SETTINGS_PASSWORD into the /settings login field, which is a prohibited action (never enter passwords into any field, even the user's own, even with authorization) regardless of the fact that .env's content was incidentally visible to me during the sed edit. Deferring this verification to the user."
